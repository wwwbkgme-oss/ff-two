[package]
# BKG: Ordnername plugin-economy, Crate-Name economy (kein Konflikt)
name        = "economy"
version     = "0.1.0"
edition     = "2021"
description = "forgefabrik.economy — Ressourcen-Ökonomie Plugin: Tokens, Budget, Throttling (cdylib)"

[lib]
crate-type = ["cdylib"]

[dependencies]
serde      = { workspace = true }
serde_json = { workspace = true }
types        = { workspace = true }
tracing      = { workspace = true }
uuid       = { workspace = true }
chrono     = { workspace = true }
