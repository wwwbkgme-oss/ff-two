[package]
name        = "devstudio-types"
version     = "0.1.0"
edition     = "2021"
description = "ForgeFabrik DevStudio — shared domain models (Single Source of Truth)"

[dependencies]
serde      = { workspace = true }
serde_json = { workspace = true }
uuid       = { workspace = true }
chrono     = { workspace = true }
rand       = { workspace = true }
