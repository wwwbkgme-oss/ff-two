[package]
name        = "drivers"
version     = "0.1.0"
edition     = "2021"
description = "runtime/drivers — Infrastruktur-Adapter (LLM I/O, Provider-Routing)"

# BKG/ARCHITECTURE.md: I/O-Layer — HTTP-Calls, API-Keys, Provider-Switching.
# Implementiert Traits aus domain/agents (LlmDriver).
# Wird von runtime/server verdrahtet — nie direkt von domain/ oder plugins/.

[dependencies]
async-trait  = { workspace = true }
tokio        = { workspace = true }
reqwest      = { workspace = true }
serde        = { workspace = true }
serde_json   = { workspace = true }
tracing      = { workspace = true }
anyhow       = { workspace = true }
errors       = { workspace = true }
agents       = { workspace = true }
