[package]
name    = "config"
version = "0.1.0"
edition = "2021"
description = "runtime/config — Settings-Loader (env-vars via dotenvy, kein externer config-crate)"

[dependencies]
dotenvy            = { workspace = true }
tracing-subscriber = { workspace = true }
serde              = { workspace = true }
anyhow             = { workspace = true }
